---
title: "Use `postCreateCommand` for `pip install` so dependencies..."
impact: MEDIUM
impactDescription: "general best practice"
tags: python, devcontainer, python-dev-container-setup, python-feature-configuration, virtual-environments
---

## Use `postCreateCommand` for `pip install` so dependencies...

Use `postCreateCommand` for `pip install` so dependencies are cached in Codespaces prebuilds.

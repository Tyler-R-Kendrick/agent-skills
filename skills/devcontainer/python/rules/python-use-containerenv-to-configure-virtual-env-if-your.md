---
title: "Use `containerEnv` to configure `VIRTUAL_ENV` if your..."
impact: MEDIUM
impactDescription: "general best practice"
tags: python, devcontainer, python-dev-container-setup, python-feature-configuration, virtual-environments
---

## Use `containerEnv` to configure `VIRTUAL_ENV` if your...

Use `containerEnv` to configure `VIRTUAL_ENV` if your project expects a venv.

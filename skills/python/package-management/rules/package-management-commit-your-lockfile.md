---
title: "Commit your lockfile"
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: python, pip, uv, poetry, pdm, conda, pipx, virtualenv, dependencies, package-management, installing-packages, managing-dependencies, choosing-between-pipuvpoetrypdmconda
---

## Commit your lockfile

Whether it is `uv.lock`, `poetry.lock`, `pdm.lock`, or a compiled `requirements.txt`, commit it to version control for reproducible builds.
